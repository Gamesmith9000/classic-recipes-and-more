<!DOCTYPE html>
<html>
  <head>
    <title>Classic Recipes & More</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" type="text/css" href='https://fonts.googleapis.com/css2?family=Lato&family=Parisienne&display=swap'>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  </head>

  <body>
    <%# Source: https://developer.paypal.com/docs/checkout/integrate/ %>
    <%# [NOTE] Instructions to say to use sandbox client id (which was done) %>
    <%# [NOTE] Disabled paypal credit (to get rid of 'pay later' button) %>

    <script src="https://www.paypal.com/sdk/js?client-id=<%= ENV[ENV['RAILS_ENV'] == 'production' ? 'PAYPAL_CLIENT_ID': 'PAYPAL_SANDBOX_CLIENT_ID']%>&disable-funding=credit"></script>

    <script>
      <%# Flash variables %>
      <%# for use in React and Devise's non-React rendered views %>

      const userDisplay = "<%= current_admin == nil ? nil : current_admin.email %>";
      const hasSignedInFlashMessage = <%= notice == successful_sign_in_message %>;
      const flashMessages = {
        alert: (<%= alert.nil? %> === true) ? null : "<%= alert %>",
        notice: <%= notice.nil? %> === true ? null : "<%= notice %>"
      }
    </script>

    <% if controller_name == 'sessions' && (alert || notice) %>
      <% if alert %>
        <p class="flash-alert"><%= alert %></p>
      <% end %>
      <% if notice %>
        <p class="flash-notice"><%= notice %></p>
      <% end %>
    <% end %>
    
    <%= yield %>
  </body>
</html>
